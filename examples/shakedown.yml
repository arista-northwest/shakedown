vars:
    _: false
    # vrf: management
    # upgrade: "flash:EOS-4.20.5F.swi"
    # downgrade: "flash:EOS-4.18.3.1F.swi"

connections:
  veos2.lab.lan:
    auth: ["admin", ""]
    transport: http
    tags: [ dut, tor ]

  veos3.lab.lan:
    tags: [ sdut, csp ]

tests:
  test_001_basics:
    software_version: 4.20.5F
